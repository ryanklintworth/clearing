<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <div class="container">
      <h1>Show Page</h1>
      <ul>
        <li><h3><%=item.name; %></h3></li>
        <li><img src="<%=item.img; %>" alt="An image of a <%= item.name %>"></li>
        <li>description: <%=item.description; %></li>
        <% if (item.qty <= 0) { %>
          <h3>OUT OF STOCK</h3>
        <% } else { %>
          <h6>Only <%=item.qty %> remining</h6>
        <% } %>
        <li>quantity: <%=item.qty; %></li>
        <li>price: <%=item.price; %></li>
      </ul>
      <button type="button" name="button">
        <a href="/">Back</a>
      </button>
    </div>
  </body>
</html>
